# API keys and secrets
.env
.env.local

# Python
__pycache__/
*.pyc
*.pyo

# 运行时日志（uvicorn 等）。曾误提交过 backend/uvicorn.{out,err}.log，
# 加规则防止再犯——日志不是源码，而且会随运行不断变化。
*.log
.venv/
venv/
.pytest_cache/
# 构建产物。vendor/ 里曾混进一份（含论文仓库 README 全文，属溯源混淆），
# 已删除并记于 vendor/nl2sql/VENDOR-PATCHES.md §2。加通配防止再带进来。
*.egg-info/

# Node / React
node_modules/
dist/
build/
.vite/
*.local

# Runtime data (regenerated by demo/seed.py)
# 注意：`**/` 前缀是必需的——单独的 `data/*.db` 只在仓库根目录生效，
# 匹配不到 backend/data/。团队规范 §4.4 要求 .db 一律不入库。
**/data/*.db
**/data/*.db-journal
# 令牌签名密钥（backend/credentials.py 首次运行时生成）。
# **绝不入库**：它一泄露，任何人都能签发以假乱真的令牌，签名就等于没有。
# 团队共享请走群消息/密码管理器，或各自用 MEDGUARD_TOKEN_SECRET 覆盖。
**/data/.token_secret

# 视觉核查输出（npm run visual）
# 两个都写：v2 开发期用的是 -v2 后缀，当时只加了 .visual-out/，
# 结果每次跑视觉核查都把 13 张 PNG（约 2MB/张）提交一次，历史里堆了
# 31.7MB 截图、并间接导致 .git 膨胀到 341MB。用通配避免再犯。
.visual-out/
.visual-out-*/


# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# 过程文件（本地 issue / spec / 审查产出）。CLAUDE.md 说 issue 与 spec 放在
# 这里，那是**工作区**不是交付物——曾经差点把 131KB 的审查 diff 一起推上去。
# 确实需要随仓库保留的条目，用 `git add -f` 单独放行。
.scratch/
.vercel
.env*
