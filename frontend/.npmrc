# npm 配置
#
# legacy-peer-deps=true —— 让 npm 不强制校验 peer 依赖。
#
# 起因：openapi-typescript@7.x 的 peer 要求是 `typescript@^5.x`，而本项目用的是
# `typescript@~6.0.2`。npm 默认遇到这种不一致会**直接拒绝安装**，于是全新克隆
# 按 README 走 `npm install` 会失败（`npm ci` 同样失败，因为 package-lock.json
# 本身就是在这个冲突下生成的）。
#
# 为什么可以安全地忽略这个冲突：
#   openapi-typescript 只有一个用途——把后端的 openapi.json 转成
#   src/api/types.ts。而 types.ts **已经生成好并入库**，它既不参与
#   `npm run build`，也不参与运行时。所以它对 TypeScript 版本的"意见"
#   对构建和运行没有任何影响。
#
# 反过来，typescript 6 是本项目**实际用于类型检查的版本**（`npx tsc -b` 通过）。
# 为了迁就一个不参与构建的依赖而把它降级，风险大、收益为零。
#
# 将来若 openapi-typescript 支持了 TypeScript 6，可以删掉这个文件。
legacy-peer-deps=true
