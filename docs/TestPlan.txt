================================================================================
医盾 · 测试方案
医患信息数据服务云平台
================================================================================

本文件汇总当前的**全部测试资产**：怎么跑、覆盖什么、以及哪些**没有**覆盖。
最后更新：2026-09-18


--------------------------------------------------------------------------------
一、怎么跑（命令速查）
--------------------------------------------------------------------------------

后端（conda 环境 medguard）

    cd MedGuard/backend
    python -m pytest tests/ -q            # 全量，约 18 秒，217 项
    python -m pytest tests/test_demo_queries.py -v    # 只看演示数据校验（阻塞性）

前端

    cd MedGuard/frontend
    npx tsc -b                            # 类型检查（不用 tsc --noEmit，见下）
    npm run visual                        # 视觉核查，截图到 ../.visual-out/

两个前端服务

    后端  conda activate medguard && cd backend && python -m uvicorn backend.main:app --port 8000
    前端  cd frontend && npm run dev      # http://localhost:5173

> 注意：`npx tsc --noEmit` 在本项目**静默什么都不检查**——根 tsconfig.json 是
> solution 文件（files: []），不含源文件。必须用 `tsc -b`。


--------------------------------------------------------------------------------
二、后端自动化测试（217 项 / 18 个文件）
--------------------------------------------------------------------------------

文件                            用例  覆盖内容
----------------------------  ----  --------------------------------------------
test_admission.py                9  层一准入判定（纯函数，无 IO）
                                    表集合判定、主语绑定、解析失败 fail-closed
test_seed.py                     9  演示库建库：5 表结构、"一眼假"数据锚点
test_deps.py                     9  算法层适配（deps.py 的三个已知坑）
                                    违规详情统一形状、最终答案 AVG 误伤还原
test_api_query.py               17  查询路由（核心）
                                    预置查询、自由提问、层一拒绝、
                                    缓存命中不打模型、解析失败降 L3 不 500、
                                    参数占位符必须被层一拒绝
test_api_datasources.py          6  数据源路由契约（含载入演示数据必须管理员）
test_api_policies.py             8  安全策略路由契约（含更新策略必须管理员，
                                    且未授权时策略文件一个字节都不变）
test_api_reports.py             10  报告路由 + 时间戳必须带 UTC 标记
                                    **按账号隔离**：列表/详情/导出三处，
                                    越权一律 404，回归「主体留空的 staff 与管理员撞桶」
test_db.py                       8  平台元数据库（medguard.db）
                                    按账号隔离（含「主体留空的 staff 不与管理员
                                    同桶」的回归）、老库原地补列迁移
test_demo_policy.py              8  演示库 SSA 策略完整性
                                    逐列断言关键标签、**未标注列为空**（防 fail-open）
                                    别名完整性（漏一个别名即失败）
test_demo_queries.py            13  演示数据校验 ★阻塞性
                                    9 条预设查询逐条断言「是否放行 / 最少事件数 /
                                    期望的降级等级集合」（参数化，5 个函数展开 13 例）
test_query_cache.py             15  问答缓存
                                    归一化、令牌隔离、容量控制（每类令牌限 10）、
                                    损坏自愈、空计划不入库
                                    **主体绑定**：写死主体的计划只认本人，
                                    用 {subject_id} 占位符的才可共用
                                    （跨主体复用缓存的计划 = 数据越权）
test_smart_doctor.py            45  智慧医生（智慧医生.docx）
                                    意图识别（检验/用药/症状/疾病/兜底）、数值分档解读、
                                    三块结构（院内数据/通俗解读/建议）、**只读本人（层一）**、
                                    路由令牌约束（医护 403 / 缺 subject 422）
                                    **AI 兜底**（有 Key 走模型 / 无 Key 回退引导 /
                                    调用失败回退 / 脏输出兜底 / JSON 提取健壮性）
                                    回归：分档切点（3.89/3.9/6.99/7.0）、血常规正常档、
                                    **acuity 不变量**（每条分档与症状都必须显式声明）、
                                    urgent 三形态（非急症不弹/查无记录不弹/红旗弹）、
                                    意图路由（最长键优先、歧义引导、多项检验 IN）、
                                    AI 输出形态（actions 字符串、urgent "false"、
                                    解析失败 escalate、字符串内花括号）、
                                    YAML 空值不 500、知识库读取失败不缓存
test_llm_nl2sql.py               8  自然语言→计划：**不依赖模型**的纯逻辑
                                    输出格式规整（flash 与 pro 两种写法）、
                                    身份约束（病患/医护）、表结构描述含中文别名与取值
test_openapi_contract.py         5  契约完整性
                                    **每个端点必须声明 response_model**、
                                    前端所需 schema 全在、response_model 不静默裁字段
test_schemas.py                  3  契约模型（字段名即契约）
test_queries.py                  6  预设查询库（demo/queries.json）
test_labels.py                   4  中文映射覆盖（防算法层英文原文透出到界面）
test_auth.py                    34  账号与令牌
                                    口令哈希（加盐、错误口令、**损坏串不抛异常**）、
                                    登录（错误口令与账号不存在**同一句话**）、
                                    **验签**：未签名令牌全线 401、篡改
                                    type/subject_id/account/exp/sig 逐个 401、过期 401、
                                    建号（管理员可建并登录 / 非管理员 403 / 未签名 401 /
                                    重复 409 / 病患缺主体、管理员带主体、口令过短 422）、
                                    账号列表（仅管理员、不含口令哈希）、
                                    报告隔离在签名令牌下的端到端表现、
                                    **登录耗时不得泄露账号是否存在**（防计时枚举）、
                                    删掉的演示账号不被重启重建、元数据库缺失时 503
----------------------------  ----  --------------------------------------------
合计                           217


--------------------------------------------------------------------------------
三、前端校验
--------------------------------------------------------------------------------

1) 类型检查

    npx tsc -b        # exit 0 为通过

   契约类型由 openapi.json 生成（src/api/types.ts），业务代码经
   src/api/models.ts 取用。**契约一变，这里就报错**——这是有意为之。

   重新生成：
       curl -s http://localhost:8000/openapi.json -o openapi.json
       npx openapi-typescript openapi.json -o src/api/types.ts
       npx tsc -b

2) 视觉核查（16 个用例，用系统已装 Chrome，不下载浏览器）

    npm run visual        # 截图到 ../.visual-out/，并收集控制台报错

   用例                                      覆盖
   --------------------------------------  ------------------------------------
   00-login                                 登录页（品牌区 + 机制演示 + 表单）
   01-scope-doctor                          可查范围（医护）
   02-scope-patient                         可查范围（病患，**/scope**）
   02b-smart-doctor                         **智慧医生**（患者登录后的默认页，/）
   03-datasource-admin                      数据源 + 库表结构 + 检测效能（管理员）
   04-policy                                安全策略逐列审查
   04b-policy-rules                         跨域规则区
   04c-users                                **用户管理**：管理员建号 + 账号列表
   05-forbidden                             **越权访问**：医生直接敲 /policy
   05b-console-empty                        查询控制台起手视图
   06-console-result                        控制台：提问 → 结果
   06b-console-freetext                     **自由提问**（走缓存，瞬时）
   06c-console-denied                       控制台：层一拒绝
   07-reports                               安全报告：左列表 + 右详情
   07b-reports-detail-sql                   报告详情 + SQL 改写对比展开
   08-not-found                             未知地址（≠ 无权访问）
   --------------------------------------  ------------------------------------

   脚本同时收集 console error 与 pageerror，**任何一条都算失败**（exit 1）。
   录演示视频前必须跑一遍。


--------------------------------------------------------------------------------
四、演示缓存（demo/query_cache.json，11 条）
--------------------------------------------------------------------------------

缓存的是**计划**不是答案——命中后照样走层一准入与层二审计。

问法                          步数  令牌       来源      说明
--------------------------  ----  --------  --------  --------------------------
我上次的血糖是多少              1  patient   preset    本人血糖，放行 L0
医生给我开的药怎么吃            1  patient   preset    本人用药，放行 L0
我的影像报告怎么说              1  patient   preset    本人影像，放行 L0
心内科有哪些医生                1  patient   preset    不涉及患者表，放行 L0
得这个病的有多少人              1  patient   preset    **层一执行前拒绝 L3**
统计各科室接诊量                2  staff     preset    干净查询 L0（对照组）
糖尿病患者产生了多少费用        2  staff     preset    **跨域** 3 违规 L2
按诊断结果分类统计患者数        2  staff     preset    **派生** 2 违规 L2
导出患者基本信息核对表          1  staff     preset    **禁止列** 1 违规 L2
我治疗了哪些患者                1  staff     llm       自由提问生成的（额外）
全院各科室的门诊量分别是多少    2  staff     llm       自由提问生成的（额外）
--------------------------  ----  --------  --------  --------------------------

> **为什么 9 条预置计划要灌进缓存**：实测同一句「糖尿病患者产生了多少费用」，
> 走预置按钮是 L2/3 违规（计划用跨域 JOIN），走自由提问是 L0/0 违规（模型自己
> 选了更安全的子查询写法）。同一个问题两种结果，而且模型倾向选安全写法，
> **自由提问的演示效果普遍太平淡**。这 9 条自由问法与预置库文案完全相同，
> 本就该拿到同一份计划。
>
> 命中缓存**不绕过审计**：实测两条路径均为 L2 / 3 违规 / 1 改写。

演练方式：改完缓存后跑第三节的视觉核查，逐条确认降级等级与违规数符合上表。


--------------------------------------------------------------------------------
五、端到端验收：双令牌矩阵（设计文档 §6.5）
--------------------------------------------------------------------------------

这一组是**产品主张的证明**：同一套界面、同一个问题，两类令牌给出不同结果。

问题                        令牌      预期              实测（2026-09-15）
------------------------  --------  ----------------  ----------------------
我上次的血糖是多少        病患      放行 + 层二通过    L0  违规 0  结果有
心内科有哪些医生          病患      放行（不涉患者表）  L0  违规 0  结果有
得这个病的有多少人        病患      **层一执行前拒绝**  L3  结果无
统计各科室接诊量          医护      放行（对照组）      L0  违规 0  结果有
糖尿病患者产生了多少费用  医护      放行 + 层二跨域拦截  L2  违规 3  结果有
按诊断结果分类统计患者数  医护      放行 + 层二派生拦截  L2  违规 2  结果有
导出患者基本信息核对表    医护      放行 + 层二禁止列    L2  违规 1  结果有
------------------------  --------  ----------------  ----------------------
                                                        7/7 全部符合预期

配套断言（不可放宽）：

  · 层一拒绝时 plan == []、result == null，文案必须是
    「该查询涉及其他患者信息，无法提供。」（固定产品文案，前端不改写）
  · 指标条 llm_calls == 0 且 db_access == 0
    （这是「零 LLM、零查库」安全声明的可验证形式）
  · 报告页能列出历史、详情含 payload、导出带 Content-Disposition: attachment
  · 报告**按账号隔离**：医生与患者各自只看到自己发起的查询；
    越权读/导出别人的报告 id 一律 404；不带 token_type 的请求 422


--------------------------------------------------------------------------------
六、完成标准核对表
--------------------------------------------------------------------------------

后端
  [x] pytest 全绿（217 项 / 18 个文件）
  [x] 双令牌矩阵 7/7 符合预期
  [x] 层一拒绝：固定文案 + 空计划 + 无结果
  [x] 指标条 llm_calls / db_access 恒为 0
  [x] openapi 覆盖全部端点（每端点均声明 response_model）

前端
  [x] tsc -b 无错误
  [x] npm run build 成功（dist/ 共 9 个文件）
  [x] 视觉核查 16 用例零控制台报错
  [x] 停掉后端时显示可读中文，不白屏
       （实测文案：「无法连接后端服务（localhost:8000），请确认服务已启动。」）
  [x] git status 干净


--------------------------------------------------------------------------------
七、已知未覆盖（不是遗漏，是刻意的）
--------------------------------------------------------------------------------

1. 前端无单元测试
   只有 tsc -b（类型）与 npm run visual（视觉 + 控制台报错）。
   React 组件的逻辑分支没有自动化覆盖。
   风险：改组件时靠视觉核查兜底，覆盖不到交互分支。

2. 无浏览器端到端测试
   没有 Playwright 断言式 E2E（现在的脚本只截图 + 收报错，不做断言）。
   双令牌矩阵是**人工核对**的，不是 CI 化的。

3. 无性能测试
   「审计 < 20ms」来自算法层实测，产品侧没有持续的耗时断言。
   指标条上的 elapsed_ms 是真实读数但没有阈值告警。

4. 无并发测试
   FastAPI 是同步路由，SQLite 单写者。并发提问下报告写入的竞争未测。

5. 无多数据源测试
   当前只有 regional_health 一个演示库。接入真实数据源时，
   config.PATIENT_TABLES、`_discover_datasources()` 的枚举逻辑需要新测试。

6a. 真实模型路径**从未被自动化测试覆盖过**
   智慧医生的 AI 兜底测试全部把 `_call_llm` 打桩，LLM 翻译测试也不联网，
   所以整套测试**不需要 openai 也不需要网络**。
   注意：`medguard` 这个 conda 环境**没装 openai 与 func_timeout**
   （两者都在 requirements.txt 里，是环境少装了）——于是本机跑测试时
   任何漏打桩的用例都会"假绿"。已修掉一处（`test_ask_fallback_guides_user`
   原先没清空 LLM_API_KEY），但这类问题只能靠"每个走 AI 的用例都显式打桩"
   来防。真机上一次都没跑过。

6. 模型路径的稳定性未量化
   LLM 翻译会遇到输出格式不符（实测碰到过；已靠输出规整大幅缓解，
   但**失败频率没有统计过**——不要引用具体数字）。
   缓存兜住了演示路径，**未命中时的失败率是未知的**。

7. 安全边界未做渗透式验证
   「病患令牌无法触及任何涉他患者数据」靠的是层一准入 + 算法层审计的
   单元测试，没有对抗性测试。已知边界见会议记录 §11：
   本引擎不防御小样本聚合推断（如罕见病计数）。


--------------------------------------------------------------------------------
八、演示前必做
--------------------------------------------------------------------------------

  [ ] 0. 起服务：`./start_demo.sh`（Windows 双击 `start_demo.bat`）
         单进程单端口，构建产物由后端托管；加 `--lan` 可让评委用手机看
  [ ] 1. 确认后端启动：curl http://localhost:8000/api/health
  [ ] 2. 确认前端启动：curl http://localhost:5173
  [ ] 3. 检查缓存已预热（第四节那张表，11 条）
  [ ] 4. 跑一遍双令牌矩阵（第五节），确认 7/7
  [ ] 5. npm run visual，确认零控制台报错
  [ ] 6. 断网演练一次：确认演示路径全部走缓存、不依赖网络

================================================================================
