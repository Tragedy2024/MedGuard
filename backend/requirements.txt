# 医患信息数据服务云平台 — 后端依赖
# 安装：pip install -r requirements.txt

# Web 框架
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# SQL AST 解析（安全审计/改写的地基，算法层要求 >=26）
sqlglot>=26.0.0

# 策略 YAML
PyYAML>=6.0

# 测试
pytest>=8.0
httpx>=0.27
