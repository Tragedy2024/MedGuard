# 医患信息数据服务云平台 — 后端依赖
# 安装：pip install -r requirements.txt

# Web 框架
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# SQL AST 解析（安全审计/改写的地基，算法层要求 >=26）
sqlglot>=26.0.0

# 策略 YAML
PyYAML>=6.0

# 中文分词（智慧医生的知识检索，见 backend/knowledge/）
# 约 20MB（含词典），首次调用有约 1 秒的词典加载——已在
# backend/main.py 的 lifespan 里预热，避免第一个提问的患者吃这一下。
jieba>=0.42

# 自然语言 → SQL（MAC-SQL 体系，已内置在 vendor/nl2sql/src/core/）
# 注意：上游 MAC-SQL 的 core/agents.py 还 import 了 pandas 与 tiktoken，
# 但两者都是死导入，已在本仓库移除以免拖入约 32MB 依赖。
# 详见 vendor/nl2sql/VENDOR-PATCHES.md。
openai>=1.0
func_timeout>=4.3
tqdm>=4.0

# .env 读取（OPENAI_API_KEY 等）
python-dotenv>=1.0

# 测试
pytest>=8.0
httpx>=0.27
