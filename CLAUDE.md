## Agent skills

### Issue tracker

Issue 与 spec 以 markdown 文件存放在 `.scratch/` 下（本地 markdown，未用 GitHub/GitLab）。详见 `docs/agents/issue-tracker.md`。

### Triage labels

沿用五个默认 triage 角色字符串，未做重命名。详见 `docs/agents/triage-labels.md`。

### Domain docs

单 context 布局：根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
